## 05线程传参详解、detach()大坑，成员函数做线程函数

### 5.1、传递临时对象作为线程参数

#### 5.1.1、要避免的陷阱（解释1）

+ **shift+F9**可以查看变量的值。
+ 分析认为是`i`不是`var`的引用，虽然写的是引用，但观察发现其实是值传递。

#### 5.1.2、要避免的陷阱（解释2）

### 5.2、临时对象作为线程参数继续讲

#### 5.2.1、线程id的概念

#### 5.2.2、临时对象构造时机抓捕

### 5.3、传递类对象、智能指针作为线程参数

### 5.4、用成员函数指针做线程函数